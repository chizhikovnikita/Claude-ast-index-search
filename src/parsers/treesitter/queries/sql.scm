; CREATE TABLE — Class
(create_table
  (object_reference) @table_name)

; CREATE FUNCTION / CREATE PROCEDURE — Function
(create_function
  (object_reference) @func_name)

; CREATE INDEX — Property
(create_index
  column: (identifier) @index_name)

; CREATE TYPE — Class
(create_type
  (object_reference) @type_name)
